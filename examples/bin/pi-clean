#!/usr/bin/env sh
set -eu

if [ -n "${TUI_VERIFIER_PI_BIN:-}" ]; then
  exec "$TUI_VERIFIER_PI_BIN" "$@"
fi

if [ -x /usr/local/bin/pi_cli/pi.real ]; then
  exec /usr/local/bin/pi_cli/pi.real "$@"
fi

exec pi "$@"
