#!/usr/bin/env sh
set -eu

if [ -n "${TERMPROOF_PI_BIN:-}" ]; then
  exec "$TERMPROOF_PI_BIN" "$@"
fi

if [ -x /usr/local/bin/pi_cli/pi.real ]; then
  exec /usr/local/bin/pi_cli/pi.real "$@"
fi

exec pi "$@"
