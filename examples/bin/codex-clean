#!/usr/bin/env sh
set -eu

if [ -n "${TERMPROOF_CODEX_BIN:-}" ]; then
  exec "$TERMPROOF_CODEX_BIN" "$@"
fi

if [ -x /usr/local/bin/codex_cli/codex.real ]; then
  exec /usr/local/bin/codex_cli/codex.real "$@"
fi

exec codex "$@"
